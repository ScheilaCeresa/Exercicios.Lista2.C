#include <iostream>

using namespace std;

//Fa�a um programa que leia a largura e o comprimento de um terreno retangular, calculando
//e mostrando a sua �rea em m�. O programa tamb�m deve mostrar a classifica��o desse terreno,
//de acordo com a lista abaixo:
//� Abaixo de 100m� = TERRENO POPULAR
//� Entre 100m� e 500m� = TERRENO MASTER
//� Acima de 500m� = TERRENO VIP


int main()
{
    float largura, comprimento, area;

    cout << "Informe a largura e comprimento de um terreno: ";
    cin >> largura;
    cin >> comprimento;

    area = largura*comprimento;

    if (area<= 100) {
        cout << "Terreno Popular com "<<area<<"m�";
    }
    else
        if (area>=500) {
            cout << "Terreno Vip com "<<area<<"m�";
        }
        else
            cout << "Terreno Master com "<<area<<"m�";

    return 0;
}